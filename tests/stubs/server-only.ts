/**
 * `server-only` パッケージのスタブ。
 *
 * `server-only` は「クライアントバンドルに混入したら import 時に例外を投げる」
 * ためのパッケージ。Next.js の外 (Vitest) で実行すると例外になってしまうため、
 * 統合テストでは何もしないモジュールに差し替える。
 * (本番のビルドでは本物が使われるので、防御は効いたまま)
 */
export {};
